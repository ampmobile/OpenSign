:{$PORT} {

    # API (strip /api)
    handle_path /api/* {
        reverse_proxy opensign-backAPI.railway.internal:8080
    }

    # Files / PDFs (DO NOT strip)
    handle /app/files/* {
        reverse_proxy opensign-backAPI.railway.internal:8080
    }

    # Everything else -> frontend
    handle {
        reverse_proxy front-end-OpenSign.railway.internal:3000
    }
}
