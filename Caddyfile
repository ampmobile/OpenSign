:$PORT {

  # API → backend
  handle_path /api/* {
    reverse_proxy opensign-backAPI.railway.internal:$PORT
  }

  # EVERYTHING ELSE → frontend
  handle {
    reverse_proxy front-end-OpenSign.railway.internal:$PORT
  }
}
