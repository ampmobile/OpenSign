:${PORT}

# API → backend
handle_path /api/* {
  reverse_proxy backopensign.railway.internal:8080
}

# Frontend → static files
handle {
  root * /app/build
  try_files {path} /index.html
  file_server
}
